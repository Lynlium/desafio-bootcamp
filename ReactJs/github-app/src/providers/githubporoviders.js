export const GithubProvider = () =>{
    fetch (process.env.REACT_APP_API).then((reponse) => reponse.json());

    }


    //método de  API export baseado no server de naruto quotes, pode não funcionar!!//
